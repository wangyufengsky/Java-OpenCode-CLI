# 周度工程报告链路。不启动 OpenCode worker，也不读取历史审查产物。
# 周报证据由本链路按周窗口重新统计 Git 生成，不读取 git-report 或 SmartESB 审查历史产物。

project:
  id: "upfs-production"
  name: "UPFS Production"
  repo: "/home/wangyufeng/workspace/upfs-production"

paths:
  out: "/home/wangyufeng/reports/weekly-engineering/2026-W26"

git:
  exclude:
    - "target/**"
    - "*.lock"
